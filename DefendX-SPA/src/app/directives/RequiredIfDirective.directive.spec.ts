/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RequiredIfDirectiveDirective } from './RequiredIfDirective.directive';

describe('Directive: RequiredIfDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredIfDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
